<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<include resource="org/springframework/boot/logging/logback/defaults.xml"/>

	<springProperty scope="context" name="APP_NAME" source="spring.application.name"
					defaultValue="stock-research-copilot-backend"/>

	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{traceId}] %logger{36} - %msg%n</pattern>
			<charset>UTF-8</charset>
		</encoder>
	</appender>

	<!-- Structured-ish key=value line for ops scraping when PROFILE=prod -->
	<springProfile name="prod">
		<appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
			<encoder>
				<pattern>{"ts":"%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}","level":"%level","traceId":"%X{traceId}","logger":"%logger{36}","msg":"%replace(%msg){'\"','\\\"'}","app":"${APP_NAME}"}%n</pattern>
				<charset>UTF-8</charset>
			</encoder>
		</appender>
		<root level="INFO">
			<appender-ref ref="CONSOLE_JSON"/>
		</root>
	</springProfile>

	<springProfile name="!prod">
		<root level="INFO">
			<appender-ref ref="CONSOLE"/>
		</root>
	</springProfile>

	<logger name="com.stockresearch.copilot" level="INFO"/>
</configuration>
